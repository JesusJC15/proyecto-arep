# 🎯 AWS DEPLOYMENT - COMMAND CHEATSHEET

**Copia-pega los comandos que necesites**

## PRE-REQUISITOS

```bash
aws --version
terraform --version
docker --version
git --version
```

... (cheatsheet commands kept in repo)
