# Guion de demo final AREP

Objetivo: mostrar flujo de triaje con evidencia trazable en 5-8 minutos.

Preparación: levantar `docker compose up --build -d` y verificar `rag/status`.

Flujo sugerido:
- Entrar como `ana.patient`, crear consulta, ejecutar triage, mostrar evidencia.
- Cambiar a `dr.suarez`, revisar caso escalado y cerrar.
